{{> layout/header }}

<div class="container mt-5">
    <h2 class="fw-bold mb-4">{{caseEntity.title}}</h2>
    <p><strong>카테고리:</strong> {{caseEntity.category}}</p>
    <p><strong>작성일:</strong> {{caseEntity.createdAt}}</p>
    <hr>
    <div style="white-space:pre-line">{{caseEntity.content}}</div>
    <hr>
    <a href="/view/posts" class="btn btn-secondary mt-2">목록으로</a>
</div>
